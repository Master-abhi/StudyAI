name: Refresh News Cache (DISABLED)

on:
  # Automated schedule disabled as requested by admin (News scraper turned off)
  # schedule:
  #   - cron: '0 * * * *'
  workflow_dispatch:
    # Allow manual trigger only if needed

jobs:
  refresh-news:
    runs-on: ubuntu-latest
    steps:
      - name: Scraper disabled notice
        run: |
          echo "Automated News Scraper is disabled. News is updated manually via Admin Panel."

